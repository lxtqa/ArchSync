version: '3.8'

services:
  archsync_proxy:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: archsync_proxy
    ports:
      - "8013:8013"
    restart: unless-stopped
    environment:
      - MCP_PORT=8013
      - MCP_TRANSPORT=http
    volumes:
      - ./diff:/diff

